const inintialState = {
    pages: [
        {
            id: 0,
            name: 'Dashboard'
        },
        {
            id: 1,
            name: 'Suport'
        }
    ],
}

const aside = (state = inintialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

// types

// actionCreators

export default aside;